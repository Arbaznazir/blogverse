<nav class="navbar navbar-expand-lg bg-body-tertiary shadow-sm">
  <div class="container-fluid">
    <%= link_to "MiniBlog", root_path, class: "navbar-brand fw-bold" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav me-auto">
        <%= link_to "Home", root_path, class: "nav-link" %>
        <%= link_to "All Posts", posts_path, class: "nav-link" %>
        <% if logged_in? %>
          <%= link_to "New Post", new_post_path, class: "nav-link" %>
        <% end %>
      </div>

      <div class="navbar-nav">
        <% if logged_in? %>
          <span class="nav-link disabled">Hi, <%= current_user.name %>!</span>
          <%= link_to "My Profile", user_path(current_user), class: "nav-link" %>
          <%= button_to "Log out", session_path, method: :delete, class: "btn btn-outline-danger btn-sm ms-2" %>
        <% else %>
          <%= link_to "Sign up", new_user_path, class: "nav-link" %>
          <%= link_to "Log in", new_session_path, class: "nav-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
